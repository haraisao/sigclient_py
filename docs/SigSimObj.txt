API List
===========
------------------------------------------------------------
sig.SigSimObj
-------------------
Descriptions
^^^^^^^^^^^^^^


Supers
^^^^^^^
   None

Variables
^^^^^^^^^^
    cmdbuf = sigcomm.SigDataCommand()
    name=name
    parts = {}
    attributes = {}
    joints={}
    updateTime=0.0
    controller = ctrl

Methods
^^^^^^^^
* __init__( name, ctrl):

* getObj():

* setAttributes( data):

* getName():

* setParts( data):

* getParts( name):

* dynamics():

* getPosition():

* setCurrentPosition( x, y, z):

* setPosition( x, y, z):

* updatePosition():

* setCurrentRotation( qw, qx, qy, qz):

* x(val=None):

* y(val=None):

* z(val=None):

* getRotation():

* setRotation(qw, qx, qy, qz, abs=1):

* setAxisAndAngle(x, y, z, ang, direct=1):

* updateRotation():

* qw(val=None):

* qx(val=None):

* qy(val=None):

* qz(val=None):

* setForce(fx, fy, fz):

* addForce(dfx, dfy, dfz):
   add force parameter

* getMass():

* setMass(val):

* setAccel(ax, ay, az):

* setTorque(x, y, z):

* setJointAngle(joint_name, angle):

* setJointQuaternion(joint_name, qw, qx, qy, qz, offset=0):

* addJointTorque(joint_name, torque):

* setJointVelocity(joint_name, vel, mx):

* setAngularVelocityToJoint(joint_name, vel, mx):

* setAngularVelocityToParts( name, vel, maxf) :

* getAllJointAngles():

* getCamFOV(camID=1):

* getCamAS(camID=1):

============================= Not Implemented....
* addForceAtPos(fx, fy, fz, px, py, pz)
* addForceAtRelPos(fx, fy, fz, px, py, pz)
* addForceToParts(parts, fx, fy, fz)
* addRealForce( fx, fy, fz)
* addRealForceAtPos(fx, fy, fz, px, py, pz)
* addRealForceAtRelPos(fx, fy, fz, px, py, pz)
* attrs() 
* checkAttrs()
* classname()
* copy(const SimObj &o)
* free_()
* getAngularVelocity(Vector3d &vec)
* getAttr(const char *name) const 
* getCamDir(Vector3d &v, int camID=1, bool requestToServer=true)
* getCameraIDs()
* getCameraLinkName(int camID=1)
* getCameraNum()
* getCameraViewPoint(Vector3d &v, int camID=1)
* getCameraViewVector(Vector3d &v, int camID=1)
* getCamLink(int camID=1)
* getCamPos(Vector3d &pos, int camID=1, bool requestToServer=true)
* getCamQuaternion(double &qw, double &qx, double &qy, double &qz, int camID=1)
* getDynamicsMode()
* getFile(int num)
* getGravityMode()
* getIsGrasped()
* getJointAngle(const char *jointName)
* getJointForce(const char *jointName, JointForce &jf1, JointForce &jf2)
* getJointPosition(Vector3d &pos, const char *jointName)
* getMainParts()
* getPartsIterator()
* getPartsPosition(Vector3d &pos, const char *partsName)
* getPointedObject(const char *speakerName, int lrFlag, int lineID, int typicalType=1)
* getPointingVector(Vector3d &vec, const char *joint1, const char *joint2) 
* getPointingVector(Vector3d &vec, int lrFlag=0)
* getVelocity(Vector3d &vec)
* id() const 
* isAttached() const 
* isAttr(const char *name)
* name() const 
* push(Attribute *attr)
* push(CParts *p)
* setAngularVelocity(double x, double y, double z)
* setAttrValue(const char *name, const char *v)
* setAttrValue(const char *name, const Value &v)
* setCamAS(double as, int camID=1)
* setCamDir(Vector3d v, int camID=1)
* setCameraViewPoint(Vector3d v, int camID=1)
* setCameraViewVector(Vector3d v, int camID=1)
* setCamFOV(double fov, int camID=1)
* setCamPos(Vector3d pos, int camID=1)
* setCollisionEnable(bool flag)
* setDynamicsMode(bool dynamics)
* setGravityMode(bool gravity)
* setRequestSener(RequestSender *s)
* setVelocity(double vx, double vy, double vz)
* toBinary(int &n)
